requirements.txt is a partial dependency list (killinchu's primary runtime deps are pinned in the Dockerfile; vsp_otel is vendored under ./vsp_otel/), so install and tests/ execution defer to this repo's dedicated CI (ci.yml, backend-scheduler.yml, osint-tests.yml, install-check.yml) with curated dependency sets. Ruff E9/F remains blocking here.
