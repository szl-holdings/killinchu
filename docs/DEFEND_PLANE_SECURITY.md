# Defend Plane Security Boundary

The public runtime accepts only bounded JSON contracts, rejects arbitrary commands and caller-supplied endpoints, isolates working state by hashed session scope, requires independent approval before rehearsal, and never enables a production effector. Secrets are not returned in APIs or receipts.
