# Verification Matrix

| Layer | Evidence |
|---|---|
| Source | Focused tests and Python compilation |
| Assembly | Explicit Docker COPY and pre-catch-all route registration |
| Deployment | Exact protected-main SHA from `/api/build-info` |
| Runtime | HTTP 200 from `/defend`, `/api/defend/status`, and `/api/defend/source` |
| Authority | Human approval required; external effectors and arbitrary commands disabled |
| Retirement | Snapshot hash, deletion readback, and persistent receipt |
